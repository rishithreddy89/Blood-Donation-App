name: calendar_app
description: A simple calendar app with task management and statistics.
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
#  flutter_inappwebview: ^6.1.5
  webview_flutter: ^4.2.2
  flutter_inappwebview: ^6.0.0
  flutter_inappwebview_ios: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
